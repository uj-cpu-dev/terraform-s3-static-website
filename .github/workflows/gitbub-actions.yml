name: Terragrunt CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terragrunt-pipeline:
    uses: uj-cpu-dev/terragrunt-pipeline/.github/workflows/ci-cd.yml
    with:
      working_dir: live/dev/s3